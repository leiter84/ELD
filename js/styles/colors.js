import * as Colors from "./colorTheme";

const bgDarker = {
  backgroundColor: Colors.themeDarker
};

const bgLighter = {
  backgroundColor: Colors.themeLighter
};

const bgPrimary = {
  backgroundColor: Colors.themePrimary
};

export { bgDarker, bgLighter, bgPrimary };
